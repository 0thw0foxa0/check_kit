#ifndef PATH_UTILS_H
#define PATH_UTILS_H

#include <string>

std::string getFilePath(const std::string& argv0,const std::string& basename); // функция для получения пути к файлу в корневой папке проекта

#endif // PATH_UTILS_H